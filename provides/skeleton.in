config BR2_ROOTFS_SKELETON_ALPINELINUX
	bool "Alpine Linux"
	select BR2_PACKAGE_SKELETON_ALPINELINUX
	help
	  Build the skeleton using the pre-built binary packages and
	  alpine-make-rootfs.

config BR2_ROOTFS_SKELETON_ARCHLINUX
	bool "Arch Linux"
	select BR2_PACKAGE_SKELETON_ARCHLINUX
	help
	  Build the skeleton using the pre-built binary packages and
	  pacstrap.
